گذاشتن پروژه در GitHub (گیت‌هاب) بهترین کار ممکن است. این کار به پروفایل شغلی شما اعتبار می‌دهد و نشان می‌دهد که شما یک متخصص فنی هستید.

اصلاً نگران نباشید، روش ساده و بدون کدنویسی (از طریق مرورگر) را به شما یاد می‌دهم.

⚠️ گام صفر (حیاتی‌ترین مرحله): پاکسازی کلیدها
قبل از آپلود، حتماً فایل پایتون (cluster_seo.py) را باز کنید و کلیدهای API خودتان را پاک کنید. نباید کلید شخصی شما در اینترنت پخش شود!

جای کلیدها بنویسید:

Python

GOOGLE_API_KEY = "YOUR_GOOGLE_API_KEY_HERE"
OPENAI_API_KEY = "YOUR_OPENAI_API_KEY_HERE"
(یعنی کاربر باید بداند که اینجا جای کلید خودش است).

گام ۱: ساخت فایل requirements.txt
برنامه‌نویس‌ها فایلی کنار کد می‌گذارند که لیست کتابخانه‌های لازم (مثل pandas و openai) تویش نوشته شده است. ۱. یک فایل متنی جدید (Notepad) باز کنید. ۲. این لیست را داخلش کپی کنید:

Plaintext

pandas
numpy
scikit-learn
plotly
sentence-transformers
openpyxl
google-generativeai
openai
۳. فایل را با نام requirements.txt ذخیره کنید (دقت کنید پسوندش txt باشد).

گام ۲: ساخت مخزن (Repository) در گیت‌هاب
۱. به سایت github.com بروید و اگر اکانت ندارید، بسازید (Sign up) و وارد شوید. ۲. گوشه بالا سمت راست، روی علامت + و سپس New repository کلیک کنید. 3. در قسمت Repository name یک اسم انگلیسی جذاب بنویسید. مثلاً: AI-SEO-Keyword-Clustering 4. تیک گزینه Public را بزنید (تا همه بتوانند ببینند). 5. تیک گزینه Add a README file را بزنید. 6. دکمه سبز Create repository را بزنید.

گام ۳: آپلود فایل‌ها
الان یک صفحه خالی می‌بینید. ۱. روی دکمه Add file (بالا سمت راست لیست فایل‌ها) کلیک کنید و Upload files را انتخاب کنید. ۲. این ۳ فایل را بکشید و داخل کادر رها کنید:

cluster_seo.py (کد پایتون)

User_Guide.html (راهنمای فارسی)

requirements.txt (لیست کتابخانه‌ها) ۳. پایین صفحه دکمه سبز Commit changes را بزنید.

گام ۴: ویترین پروژه (فایل README)
الان فایل‌ها آپلود شدند، اما صفحه اصلی پروژه شما توضیحات جذابی ندارد. بیایید آن را حرفه‌ای کنیم.

۱. در صفحه اصلی پروژه‌تان در گیت‌هاب، روی فایل README.md کلیک کنید. ۲. روی آیکون مداد ✏️ (Edit) کلیک کنید. ۳. هر چه آنجا هست پاک کنید و متن زیر را کپی و آنجا Paste کنید. (من این متن را مخصوص شما و با لینک‌های شما نوشتم):

Markdown

# 🚀 AI SEO Keyword Clustering Tool (Farsi Support)

این ابزار با استفاده از هوش مصنوعی (Google Gemini 2.0 & OpenAI) و مدل‌های ریاضی (BERT)، لیست‌های طولانی کلمات کلیدی را به **استراتژی محتوای عملیاتی** تبدیل می‌کند.

## ✨ ویژگی‌ها
- **خوشه‌بندی دقیق:** استفاده از مدل `BERT` برای درک ریاضی شباهت کلمات.
- **تحلیل استراتژیک:** استفاده از `Gemini 2.0` یا `GPT-4` برای نام‌گذاری خوشه‌ها.
- **تشخیص نیت کاربر:** تشخیص خودکار Intent (خرید، آموزش، و...).
- **خروجی گرافیکی:** ارائه نقشه تعاملی HTML برای پرزنت به کارفرما.
- **کاملاً رایگان:** پشتیبانی از نسخه رایگان API گوگل.

## 📦 پیش‌نیازها
برای اجرای این اسکریپت به پایتون نیاز دارید. کتابخانه‌ها را نصب کنید:

bash
pip install -r requirements.txt
🚀 نحوه اجرا
فایل cluster_seo.py را باز کنید.

کلید API خود (Google یا OpenAI) را در خطوط ابتدایی جایگذاری کنید.

فایل را اجرا کنید.

فایل اکسل keywords_input.xlsx ساخته می‌شود. کلمات خود را وارد کنید.

مجدد برنامه را اجرا کنید و از خروجی لذت ببرید!

📬 ارتباط با توسعه‌دهنده
اگر سوالی داشتید یا باگی پیدا کردید، خوشحال می‌شم باهام در ارتباط باشید:

LinkedIn: [Amir Dehghani](https://www.linkedin.com/in/amirghf/)

Instagram: @alef.ba.dal

Developed with ❤️ for the SEO Community.
